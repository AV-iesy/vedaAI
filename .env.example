# The first milestone runs with a deterministic demo extraction provider.
# Add provider credentials when the production adapters are implemented.
ANTHROPIC_API_KEY=
OCR_PROVIDER=demo
AWS_REGION=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
